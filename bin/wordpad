#!/bin/sh

WORDPAD=$(echo "$PROGRAMFILES/Windows NT"/*/wordpad.exe)

test $# = 1 &&
case "$1" in
\\*|/*|?:*) ;; # do nothing
*) set "$(pwd)/$1";;
esac

test -x "$WORDPAD" && exec "$WORDPAD" "$@"

echo "Could not launch $WORDPAD"
exit 1
