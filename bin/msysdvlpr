#!/bin/sh
# This file will start another MSYS session as an MSYS system instead of a
# MINGW32 system. I.E.: uname -s will return MSYS_NT-4.0 instead of
# MINGW32_NT-4.0.
# 
cd /
cmd //c start /msys.bat MSYS
